/** @deprecated Import from messaging-client (UI) or messaging-host (background). */
export { sendMessage, sendMessageToTab } from './messaging-client';
export { broadcastSettingsChanged, onMessage } from './messaging-host';
